<h3>Show Category</h3>
<%= link_to @category.name %>

<ul>
  <% @category.posts.each do |category| %>
    <li><%= category.body %> </li>
    <%= link_to @category.name, category_path(category) %>
  <% end %>
</ul>